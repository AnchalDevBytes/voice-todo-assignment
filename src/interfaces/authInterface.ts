export interface SigninResponse {
  id: string;
  name: string;
  email: string;
  createdAt: string;
  updatedAt: string;
}

export interface SignupResponse {
  id: string;
  name: string;
  email: string;
  createdAt: string;
  updatedAt: string;
}
